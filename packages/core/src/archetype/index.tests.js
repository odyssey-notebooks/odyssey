import { Archetype, newArchetype } from './index.js'

describe('exports', () => {
  it('has all expected exports defined', () => {
    expect(Archetype).toBeDefined()
    expect(newArchetype).toBeDefined()
  })
})
